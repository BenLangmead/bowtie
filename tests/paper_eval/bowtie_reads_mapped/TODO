1) [ ] add one or two new data series (I think one is OK) for extension mode 
2) [ ] make a second graph representing bases mapped (rather than reads mapped) 
3) [ ] get gnuplot to draw a full y axis on the RHS
